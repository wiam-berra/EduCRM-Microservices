server:
  port: 8085

spring:
  application:
    name: ai-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

services:
  student-url: http://STUDENT-SERVICE
  attendance-url: http://ATTENDANCE-SERVICE